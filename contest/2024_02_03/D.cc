#include <bits/stdc++.h>
#include <iostream>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)

int main(){
    int n;
    cin >> n;
    string s[n];
    rep(i, n) cin >> s[i];

    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            if()
        }
    }
    return 0;
}