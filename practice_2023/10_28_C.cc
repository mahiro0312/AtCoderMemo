#include <iostream>
using namespace std;
static const int N_MAX = 3000000, M_MAX = 1000000000;

int main(){
    int N, M;
    int A[M_MAX];

    //読み取り
    cin >> N >> M;

    //for文でAの座標を読み取り
    for(int i=0;i<N;i++){
        cin >> A[i];
    }
    
    //どう最大値を得るか

    return 0;
}